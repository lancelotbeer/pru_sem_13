<h1>pag.index</h1>


<%= link_to 'crear', todos_new_path %>

<% @todos.each do |t| %>
<div class = "well">
  <%= t.description %>

  <%= t.completed %>
  <%= link_to 'show', t %>
  <%= link_to 'edit', edit_path(t) %>
</div>
<% end %>
